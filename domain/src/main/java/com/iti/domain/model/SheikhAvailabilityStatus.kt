package com.iti.domain.model

/** Matches the backend's `AVAILABLE | BUSY | OFFLINE` contract exactly (docs/API-Contract.md §13.1). */
enum class SheikhAvailabilityStatus { AVAILABLE, BUSY, OFFLINE }

